name: Docker Build And Push

on:
  push:
    branches:
      - 'main'

jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
    
      - name: "⤵️ Clone the product repo"
        uses: actions/checkout@v3
        with:
          path: 'this_repo'
          ref: ${{ github.ref_name }}
          
      - name: "🛠 Build and push dockerfile(s)"
        run: |
          DEPLOYREPO=${GITHUB_REPOSITORY##*/}
          DEPLOYREPOLOWER=${DEPLOYREPO,,}
            for d in $GITHUB_WORKSPACE/this_repo/*/; do
              echo "$d"
              REPONAME=${d##*/}
              echo "$REPONAME"
              echo -= 💻 Found "$FILENAME" =-
              cd "$d"
              echo -= 💻 Login to ECR via Docker client =-
              docker login -u sfriedel --password-stdin
              echo -= 💻 Build "$FILENAME" =-
              docker build -t $ECRREPO:latest -f $FILENAME .
              echo -= 💻 Push "$FILENAME" =-
              docker push sfriedel/openvscode-plus:latest
              docker push $ECRREPO:latest
            done

